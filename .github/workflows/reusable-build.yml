name: Reusable Build

on:
  workflow_call:
    inputs:
      image-name:
        required: true
        type: string
      registry:
        required: true
        type: string
      username:
        required: true
        type: string
    secrets:
      REGISTRY_TOKEN:
        required: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::960451805205:role/GitHubActionsRole
          aws-region: ap-southeast-1

      - name: Test AWS Access
        run: aws sts get-caller-identity

      - uses: docker/setup-buildx-action@v3

      - name: Login
        run: echo ${{ secrets.REGISTRY_TOKEN }} | docker login ${{ inputs.registry }} -u ${{ inputs.username }} --password-stdin

      - name: Build and Load Local Image
        uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          tags: local-scan-target:test
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: "image"
          image-ref: local-scan-target:test
          format: "table"
          exit-code: 0
          ignore-unfixed: true
          severity: CRITICAL,HIGH

      - name: Push Image to Registry
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ inputs.registry }}/${{ inputs.image-name }}:${{ github.sha }}
            ${{ inputs.registry }}/${{ inputs.image-name }}:latest
          cache-from: type=registry,ref=${{ inputs.registry }}/${{ inputs.image-name }}:cache
          cache-to: type=registry,ref=${{ inputs.registry }}/${{ inputs.image-name }}:cache,mode=max,image-manifest=true
